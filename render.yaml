services:
  - type: web
    name: electrofrio-backend
    env: php
    buildCommand: composer install && php artisan key:generate && touch database/database.sqlite && php artisan migrate --force
    startCommand: php artisan serve --host=0.0.0.0 --port=$PORT